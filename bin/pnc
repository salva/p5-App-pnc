#!/usr/bin/perl

use strict;
use warnings;
use App::pnc;

if (@ARGV == 1 and $ARGV[0] eq '-V') {
    App::pnc::version();
}
elsif (@ARGV == 2) {
    App::pnc::netcat4(@ARGV);
}
else {
    die "Usage:\n    $0 host port\n\n";
}
